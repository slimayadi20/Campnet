{% extends 'back.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>Voici le nombre de personnes</th>
                <td>{{ reservation.nbrPers }}</td>
            </tr>
            <tr>
                <th>Date d'arrivée</th>
                <td>{{ reservation.date ? reservation.date|date('d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>Date de retour</th>
                <td>{{ reservation.dateR ? reservation.dateR|date('d-m-Y') : '' }}</td>

            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_index') }}">back to list</a>

    <a href="{{ path('reservation_edit', {'id': reservation.id}) }}">edit</a>
    <a href="{{ path('pdf2', {'id': reservation.id}) }}">télécharger pdf</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
